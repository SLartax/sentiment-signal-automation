# ===========================================
# FILE DI ESEMPIO PER CONFIGURAZIONE SECRET
# ===========================================
# Questo file mostra quali variabili d'ambiente sono necessarie.
# NON inserire qui valori reali - configura i secret in GitHub Settings.

# --- OpenAI Configuration ---
# La tua API key di OpenAI (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...your-key-here...

# Modello OpenAI da utilizzare (opzionale, default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# --- Email Configuration ---
# Email del mittente (quella da cui viene inviato il segnale)
SENDER_EMAIL=tuaemail@gmail.com

# Password dell'app Gmail o password dell'account email
# Per Gmail: usa una "App Password" generata nelle impostazioni di sicurezza
# https://myaccount.google.com/apppasswords
SENDER_PASSWORD=your-app-password-here

# Email del destinatario (dove riceverai il segnale)
# Default: studiolegaleartax@gmail.com
RECIPIENT_EMAIL=studiolegaleartax@gmail.com

# --- SMTP Server Configuration ---
# Server SMTP (default: smtp.gmail.com per Gmail)
SMTP_SERVER=smtp.gmail.com

# Porta SMTP (default: 587 per TLS)
SMTP_PORT=587

# ===========================================
# ISTRUZIONI PER CONFIGURARE I SECRET
# ===========================================
#
# 1. Vai su: https://github.com/SLartax/sentiment-signal-automation/settings/secrets/actions
# 2. Clicca su "New repository secret"
# 3. Aggiungi ogni variabile sopra come secret separato
# 4. Usa esattamente gli stessi nomi delle variabili
#
# I secret saranno disponibili al workflow GitHub Actions
# senza essere visibili nel codice o nei log.
#
# ===========================================
